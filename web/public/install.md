ClawPlay Install Guide

1. npm install -g clawplay@latest

2. Use Bash / Write-like tools, or create a soft link, to add the following directory to your own skills configuration, So you can automatically discover skills in that directory:
   ${HOME}/.clawplay/skills

3. Then run: clawplay setup --agent
